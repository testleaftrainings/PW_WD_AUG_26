Agenda for Week7 Day2 

Service Now using postman
Different types of variable setting in API testing


Service Now
-----------

ServiceNow is an IT Service Management (ITSM) tool — it helps companies handle things like:

Reporting IT issues (e.g., “system issue”)
Requesting new hardware/software
Tracking incidents, changes, and problems
Automating workflows across departments

It's like a one-stop system for all service requests within a company — IT, HR, facilities, 
finance, etc.

ServiceNow is a platform used by organizations to manage and automate 
service requests and business workflows.

Login to ServiceNow -Developer link:
------------------------------------
 

Service instance , username and password :
------------------------------------------

instance url: https://dev280550.service-now.com

username : admin
password : hb0GX^9cgW$X



Steps to generate an OAuth token:
---------------------------------

1. Log in to your ServiceNow instance with an account that has the required administrative 
permissions. 

2. Click All → System OAuth → Application Registry → New

3. Then choose Create an OAuth API endpoint for external clients
->enter Name ex: Demo and change scope restriction to Broadly scoped->submit

4. ServiceNow documents this as the setup for inbound OAuth clients.
now select the oAuth name Demo->you should be able to see client id and client secret

Client ID: 4793d5b11a764e3880f65aec994980c4

Client Secret: N!rz@KXSj1[6v^LtI`ZRKbXSIDD9RKaj
 
 
In Postman, create a POST request to the token endpoint:

https://YOUR_INSTANCE.service-now.com/oauth_token.do

Under Body → x-www-form-urlencoded, for a user-credential/password grant setup, provide:
grant_type     password
client_id      YOUR_CLIENT_ID
client_secret  YOUR_CLIENT_SECRET
username       YOUR_SERVICENOW_USERNAME
password       YOUR_SERVICENOW_PASSWORD
 

Request Payload :
----------------

{
  "short_description": "Email not working",
  "description": "User cannot send or receive emails",
  "comments": "Updated via POSTMAN",
  "work_notes": "Checked mail server logs",
  "subcategory": "Email",
  "impact": "2",
  "urgency": "2"
}


>> Service Now Navigation to Create Incidents using UI interface and API Interface within servicenow :

--> UI => All -> Type Incident  (in Filter text box) --> Choose Incident from Service Desk
--> REST API => All -> REST API Explorer --> Choose Create a record POST --> Choose tableName = Incident and Under 
Request Body --> Choose Short Description and fill a short description.


Classroom:

Create the change request in ServiceNow




instance url: https://dev280550.service-now.com

username : admin
password : hb0GX^9cgW$X


Different types of variable setting in API testing (Postman):
------------------------------------------------------------

1) Global Variables

Used across all collections and environments.
Example: Token, common URL.

let responsebody = pm.response.json();
let InstUrl = responsebody.instance_url;
pm.globals.set("Ins_URL", InstUrl);

EX: {{Ins_URL}}

2) Environment Variables

Used for specific environments like Dev, QA, Prod.
Example: Lead ID, base_url, username.

let LeadResponse = pm.response.json();
let LDres = LeadResponse.id;
pm.environment.set("Lead_ID", LDres);

EX: {{Lead_ID}}

3) Collection Variables

Available only inside one collection.
Used for shared values within that API project.

let responsebody = pm.response.json();
let AccessToken = responsebody.access_token;
pm.collectionVariables.set("Acc_Token", AccessToken);

Ex: {{Acc_Token}}